/**
 * hold all state specific data
 */
const state = {

    /**
     * object where to store state global scole
     */
    data : {
        // score
        score : 0,
        life: 3
    },

    // a reference to the texture atlas
    texture : null
};

export default state;
